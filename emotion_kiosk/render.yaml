services:
  - type: web
    name: emotion-kiosk-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.index:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
